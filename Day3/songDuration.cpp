#if we wan to get a odd num as return then make as add one odd + one even = then ans === odd,
#for even that is ....... even + even
#if even n/2 else n/2 and n/2...


int songDuration(int a,int b,int c) {
    return (a + c) % 2;
}

//why not b bcs its alwz even bcs 2 right so for 1 and 3 is can be max odd so
//bcs here needed that the  song one min two min three min hv to set so for proper needed a+b % c


